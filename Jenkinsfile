pipeline {
    agent any

    stages {
        stage('Préparation') {
            steps {
                echo 'On prépare le build...'
            }
        }
        stage('Build') {
            steps {
                echo 'On build...'
            }
        }
        stage('Tests') {
            steps {
                echo 'On lance les tests...'
            }
        }
    }
}
